---
import Layout from '~/layouts/Layout.astro';
import { getHomePermalink } from '~/utils/permalinks';
import Emoji from 'astro-emoji';

const title = `Error 404`;
---

<Layout metadata={{ title }}>
  <section data-aos="flip-up" data-aos-delay="150" class="flex items-center h-full p-16">
    <div class="container flex flex-col items-center justify-center px-5 mx-auto my-8">
      <div class="max-w-md text-center">
        <h2 class="mb-8 font-bold font-heading text-9xl">
          <span class="sr-only">Error</span>
          <Emoji symbol="💩" label="Shit" />
          <span class="text-primary">404</span>
        </h2>
        <p class="text-3xl font-semibold md:text-3xl">Hoppla, das sollte nicht passieren.</p>
        <p class="mt-4 mb-8 text-lg text-muted dark:text-slate-400">
          Aber keine Sorge, auf meiner Homepage findest du viele andere Dinge.
        </p>
        <a rel="noopener noreferrer" href={getHomePermalink()} class="btn ml-4">Zurück zur Startseite</a>
      </div>
    </div>
  </section>
</Layout>
